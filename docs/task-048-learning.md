# TASK 048 — Learning Loop

```
PREDICT → LOCK → OBSERVE → SETTLE → AUTOPSY → BACKWARD SEARCH
→ LEARNING CASE → PATTERN → HYPOTHESIS → FUTURE MODEL CANDIDATE
```

LearningCase status: `OBSERVATION_ONLY`, `auto_applied: false`.

Patterns require n ≥ 20 before leaving `INSUFFICIENT_N`.

No automatic weight changes. Candidates require TRAIN/VAL/TEST/HOLDOUT gates (future tasks).
