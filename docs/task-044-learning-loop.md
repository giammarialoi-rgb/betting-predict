# TASK 044 — Learning loop

Closed loop without retro mutation:

Observation → Prediction → Lock → Monitor (POST_LOCK view) → Settlement → Autopsy → Learning candidate → Model experiment (future) → Out-of-sample → New version for **future** events only.
