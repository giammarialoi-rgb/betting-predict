# TASK 046 — Data lineage

UI → Next.js route handlers → `buildControlCenter046` / `buildEventDetail046` → Lab B JSONL under `audit/external/task-044`.

Credit display reads Lab A `credit-state.json` for accounting only (shared API budget file) — no writes from UI paths.

Lab A `decisions.jsonl` never written by control-046.
