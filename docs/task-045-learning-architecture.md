# TASK 045 — Learning architecture

error → learning case → error pattern → challenger hypothesis → walk-forward → validation → holdout → human promotion

Never: single loss → mutate MODEL_v1.

MODEL_v1 immutable; challengers are new versions only.
