# TASK 054 — 24/7 operation

## Independent of Cursor

1. `pnpm permanent-live:supervisor:install`
2. Log off/on or run `pnpm permanent-live:supervisor:start`
3. Verify: `pnpm permanent-live:supervisor:status` and `pnpm permanent-live:health`

## What keeps running

| Process | Responsibility |
|---------|----------------|
| Supervisor | Heal loop ~2 min, journal, single spawn |
| Worker | Discover → analyze → lock → settle → autopsy (brain cycle) |

## Browser

Observational only — `/actuarial-lab/live-total` and `/health` use disk APIs (`api_calls_ui=0`).

## Capital

PAPER_ONLY · REAL_MONEY=false · AUTO_PROMOTION=false · open_task_055=false
