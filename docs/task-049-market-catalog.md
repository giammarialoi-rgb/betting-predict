# TASK 049 — Market catalog

Dynamic catalog from observed Odds API markets only.

Requested markets string (default): `h2h,spreads,totals`.

Mapped keys include 1X2, AH, OU, H2H_LAY, BTTS, DNB, DC when provider returns them.

Never invent markets not in the payload.
