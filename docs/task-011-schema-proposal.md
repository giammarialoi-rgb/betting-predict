# TASK 011 — Schema proposal (NOT applied)

Additive candidates for a future task **after approval**:

| Table | Purpose |
| --- | --- |
| `market_definitions` | Persist catalog beyond TS constants |
| `provider_market_map` | Verified provider→market coverage |
| `prediction_runs` / `predictions` | Durable blind research runs |
| `model_versions` | Champion/challenger registry |
| `experiment_runs` | Walk-forward experiment bookkeeping |

**Do not apply** until explicitly approved. TASK 011 proceeds without these tables.
