# TASK 044 — Learning protocol

1. Autopsy (success + failure) → optional `LEARNING_CANDIDATE` status `OBSERVED`
2. Aggregate into `learning-cases.jsonl`
3. Error pattern mining → `INSUFFICIENT` until n≥20
4. Promotion path only: TRAIN → VAL → TEST → HOLDOUT → significance → human review
5. **Never** auto-promote `MODEL_v1`
6. Benchmark remains `MARKET_DEVIG`
